{
  "identifier": "default",
  "description": "Default capabilities for MockupStudio",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "fs:default"
  ]
}
